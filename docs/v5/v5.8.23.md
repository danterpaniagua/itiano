# v5.8.23 — Report: tickets ordered by most recently updated

## Goal

In `/time/report/` Status History, show the most recently updated tickets first instead of the current fixed order.

## In Scope

- `DailyReportView` — sort `filtered_tickets` by most recent `JiraEvent.received_at` descending before building the pivot and Gantt rows

## Out of Scope

- Column order in the Status History pivot
- Ticket timeline page ordering
- Any other view or section

## Technical Changes

- `timetracking/views.py` — after `filtered_tickets` is resolved, sort by latest event timestamp descending

## Acceptance Criteria

- [ ] Status History rows in `/time/report/` show the ticket with the most recent Jira activity first
- [ ] Gantt rows follow the same order

## Validation Plan

- Open `/time/report/` and confirm the ticket with the latest event appears at the top
- Verify order is consistent between Status History and Gantt sections

## Related Scope Dependencies

- Depends on v5.8.22 (ticket filtering by `assignee_account_id`)
